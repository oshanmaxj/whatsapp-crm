const r=require('express').Router(),auth=require('../middleware/auth.middleware'),p=require('../middleware/permission.middleware'),c=require('../controllers/aiProvider.controller');r.use(auth.authenticate);r.get('/',p('ai_providers.view'),c.list);r.post('/',p('ai_providers.manage'),c.create);r.patch('/:id',p('ai_providers.manage'),c.update);r.delete('/:id',p('ai_providers.manage'),c.remove);r.post('/:id/test',p('ai_providers.test'),c.test);r.post('/:id/default',p('ai_providers.manage'),c.setDefault);r.delete('/:id/key',p('ai_providers.manage'),c.removeKey);module.exports=r;

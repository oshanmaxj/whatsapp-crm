const r=require('express').Router(),auth=require('../middleware/auth.middleware'),c=require('../controllers/commission.controller');r.use(auth.authenticate);r.get('/dashboard',c.dashboard);r.get('/transactions',c.list);r.get('/rules',c.rules);r.post('/rules',c.createRule);r.patch('/rules/:id',c.updateRule);r.post('/transactions/:id/:action',c.action);r.get('/payouts',c.payouts);r.post('/payouts',c.createPayout);r.post('/payouts/:id/:action',c.payoutAction);module.exports=r;

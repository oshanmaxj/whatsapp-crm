const r=require('express').Router(),auth=require('../middleware/auth.middleware'),p=require('../middleware/permission.middleware'),c=require('../controllers/reminderSequence.controller');r.use(auth.authenticate);r.get('/dashboard',p('reminder_sequences.view'),c.dashboard);r.get('/subscriptions',p('reminder_sequences.view'),c.subscriptions);r.get('/executions',p('reminder_sequences.view'),c.executions);r.post('/executions/:id/retry',p('reminder_sequences.retry'),c.retry);r.post('/subscriptions',p('reminder_sequences.subscribe'),c.subscribe);r.post('/subscriptions/:id/:action(pause|resume|cancel|unsubscribe)',p('reminder_sequences.unsubscribe'),c.change);r.get('/',p('reminder_sequences.view'),c.list);r.post('/',p('reminder_sequences.create'),c.create);r.get('/:id',p('reminder_sequences.view'),c.get);r.patch('/:id',p('reminder_sequences.edit'),c.update);r.delete('/:id',p('reminder_sequences.delete'),c.remove);module.exports=r;

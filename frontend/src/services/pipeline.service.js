import api from'./api';export const getPipeline= p=>api.get('/leads/pipeline',{params:p});export const getStages=()=>api.get('/pipeline/stages');export const moveLeadStage=(id,p)=>api.patch(`/leads/${id}/stage`,p);export const getLeadHistory=id=>api.get(`/leads/${id}/history`);export const getLostReasons=()=>api.get('/lost-reasons');export const getFollowups=p=>api.get('/followups',{params:p});export const createFollowup=p=>api.post('/followups',p);export const completeFollowup=(id,p)=>api.patch(`/followups/${id}/complete`,p);export const rescheduleFollowup=(id,p)=>api.patch(`/followups/${id}/reschedule`,p);export const cancelFollowup=(id,p)=>api.patch(`/followups/${id}/cancel`,p);
